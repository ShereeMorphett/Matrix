# src/meson.build
# Since there are no .cpp files, no sources are specified here.
# Just ensure the header files are properly included in the build.

# If you are building an executable:
# executable('my_executable', sources, include_directories: include_directories('../include'))